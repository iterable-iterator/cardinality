import gele from './gele.js';

/**
 * Gtle.
 *
 * @param {number} m
 * @param {number} n
 * @param {Iterable} iterable
 */
export default function gtle(m, n, iterable) {
	return gele(m + 1, n, iterable);
}
